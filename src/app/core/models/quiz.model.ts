export interface Quiz {
    title: string;
    questions: string; // semicolon-separated string
    options: string;   // semicolon-separated groups with commas
    answers: string;   // semicolon-separated correct answers
  }
  